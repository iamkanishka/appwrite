# Installation

Appwrite is published on [Hex](https://hex.pm/packages/appwrite). Add it to your list of dependencies in
`mix.exs`:

```elixir
# mix.exs
def deps do
  [
    {:appwrite, "~> 0.1.2"}
  ]
end
```